version: 2.0
jobs:
  build:
    docker:
      - image: circleci/python:2.7 
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            pip install --user -r requirements.txt
            python tests.py  
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
